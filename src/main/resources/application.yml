
server.port: 8090

provider:
  coinMarketCap.baseUrl: "https://api.coinmarketcap.com"
  onVista.baseUrl: "https://www.onvista.de"

logging.level.com.github.spartusch.rateretriever: DEBUG

spring:
    cache:
        cache-names: StockExchangeRateCache, CoinMarketRateCache
        caffeine:
            spec: expireAfterWrite=60s
