<configuration>
    <property name="logFile" value="simple-rate-retriever.log"/>

    <appender name="stdoutAppender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d %level: %message%n</pattern>
        </encoder>
    </appender>

    <appender name="logFileAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logFile}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${logFile}.%d{yyyy-MM}</fileNamePattern>
            <maxHistory>90</maxHistory>
            <totalSizeCap>5MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d [%thread] %level %logger{35}: %message%n</pattern>
        </encoder>
    </appender>

    <logger name="com.github.spartusch.rateretriever" level="info"/>

    <root level="info">
        <appender-ref ref="stdoutAppender"/>
        <appender-ref ref="logFileAppender"/>
    </root>
</configuration>